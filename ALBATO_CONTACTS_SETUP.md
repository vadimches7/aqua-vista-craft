# Настройка Albato для создания контактов в amoCRM

## ✅ Что исправлено в коде

1. **Добавлено поле имени в калькулятор** - теперь пользователь может ввести своё имя
2. **Улучшен формат данных** - добавлены поля `first_name`, `last_name` для совместимости с amoCRM
3. **Добавлено поле `message`** - все дополнительные данные (тип проекта, литраж, стоимость, адрес) собираются в сообщение для сделки

## 📋 Формат данных, которые отправляются в Albato

```json
{
  "name": "Иван Иванов",           // Полное имя
  "first_name": "Иван",            // Имя (для amoCRM)
  "last_name": "Иванов",           // Фамилия (для amoCRM)
  "phone": "+79991234567",         // Телефон (обязательно)
  "email": "ivan@example.com",     // Email (если есть)
  "source": "bio-cube.ru",         // Источник
  "page_url": "https://...",       // URL страницы
  "form": "calculator",            // Тип формы
  "project_type": "Установка под ключ",  // Для калькулятора
  "volume": "100-250 л",          // Для калькулятора
  "calculated_price": "180 000 ₽", // Для калькулятора
  "address": "Москва, ...",        // Для калькулятора
  "tariff_title": "Reef 240 Start", // Для тарифов
  "tariff_price": "от 120 000 ₽",  // Для тарифов
  "message": "Тип проекта: ...\nЛитраж: ...\n..." // Собранное сообщение
}
```

## 🔧 Настройка связки в Albato

### Шаг 1: Проверьте настройки "Входящий вебхук"

1. Откройте вашу связку в Albato
2. Найдите шаг "Входящий вебхук"
3. Убедитесь, что все поля правильно распознаются

### Шаг 2: Настройте шаг "Создать контакт" в amoCRM

В шаге создания контакта в amoCRM нужно правильно сопоставить поля:

**Обязательные поля:**
- **Имя контакта** → `name` или `first_name`
- **Телефон** → `phone`
- **Email** → `email` (если есть)

**Рекомендуемые поля:**
- **Имя** → `first_name`
- **Фамилия** → `last_name`
- **Телефон рабочий** → `phone`
- **Email рабочий** → `email`

### Шаг 3: Настройте шаг "Создать сделку" в amoCRM

В шаге создания сделки:

**Обязательные поля:**
- **Название сделки** → `name` или `"Заявка с сайта — " + form`
- **Ответственный** → ваш пользователь amoCRM

**Дополнительные поля:**
- **Комментарий/Примечание** → `message` (содержит все данные формы)
- **Источник** → `source` ("bio-cube.ru")
- **URL страницы** → `page_url`

### Шаг 4: Свяжите контакт и сделку

В настройках связки убедитесь, что:
1. Сначала создается контакт
2. Затем создается сделка
3. Сделка привязывается к контакту (через поле "Контакт" в сделке)

## 🧪 Тестирование

1. Отправьте тестовую заявку с сайта
2. Проверьте в Albato "Ловец вебхуков" - должны быть все поля
3. Проверьте в amoCRM:
   - Создался ли контакт с именем и телефоном
   - Создалась ли сделка
   - Привязана ли сделка к контакту

## ❓ Если контакты всё ещё не создаются

### Проверьте в Albato:

1. **Логи выполнения связки** - там будут видны ошибки
2. **Настройки полей** - убедитесь, что поля правильно сопоставлены
3. **Права доступа** - у интеграции amoCRM должны быть права на создание контактов

### Проверьте в amoCRM:

1. **Права пользователя** - должен иметь права на создание контактов
2. **Настройки полей** - убедитесь, что поля `first_name`, `phone` существуют
3. **Логи** - проверьте логи amoCRM на ошибки

### Альтернативное решение:

Если проблема в маппинге полей, можно:
1. В Albato использовать действие "Изменить данные" перед созданием контакта
2. Переименовать поля в нужный формат
3. Объединить `first_name` и `last_name` в `name` если нужно

## 📝 Пример правильной настройки в Albato

```
Шаг 1: Входящий вебхук
  → Получает данные от сайта

Шаг 2: Изменить данные (опционально)
  → Переименовать поля если нужно
  → name → first_name + last_name

Шаг 3: Создать контакт в amoCRM
  → Имя: first_name
  → Фамилия: last_name
  → Телефон: phone
  → Email: email

Шаг 4: Создать сделку в amoCRM
  → Название: "Заявка с сайта — " + form
  → Контакт: [ID контакта из шага 3]
  → Комментарий: message
  → Источник: source
```

## 🔍 Отладка

В консоли браузера (F12) при отправке формы вы увидите:
```
[Albato] Sending request to webhook: { url: ..., payload: {...} }
[Albato] Sending form data: { name: "...", phone: "...", ... }
[Albato] Response status: 200 OK
```

Проверьте, что в `payload` есть все нужные поля, особенно `name` и `phone`.

